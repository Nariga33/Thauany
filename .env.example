# Preenchido automaticamente pela Vercel quando você conecta um banco
# Postgres/Neon ao projeto (aba "Storage" -> "Create Database" -> Postgres).
# Para rodar localmente: vercel env pull .env.local
DATABASE_URL=
